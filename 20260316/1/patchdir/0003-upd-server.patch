From 603dcac2c4ceb900a7ec0e9d41d32c73b3e6ce05 Mon Sep 17 00:00:00 2001
From: Anya <anya@MacBook-Pro-Anya.local>
Date: Fri, 20 Mar 2026 00:11:03 +0300
Subject: [PATCH 3/3] upd server

---
 20260316/1/server.py | 116 +++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 116 insertions(+)

diff --git a/20260316/1/server.py b/20260316/1/server.py
index e69de29..cec7c0d 100644
--- a/20260316/1/server.py
+++ b/20260316/1/server.py
@@ -0,0 +1,116 @@
+import sys
+import cmd
+import io
+import socket
+import shlex
+import cowsay
+import readline
+import asyncio
+
+class Player:
+    def __init__(self):
+        self.x = 0
+        self.y = 0
+
+    def move(self, x1, y1):
+        self.x = (self.x + x1) % 10
+        self.y = (self.y + y1) % 10
+        return self.x, self.y
+
+
+class Monster:
+    def __init__(self, name, word, hp):
+        self.name = name
+        self.word = word
+        self.hp = hp
+
+class Game:
+    def __init__(self):
+        self.field = [[None] * 10 for _ in range(10)]
+        self.player = Player()
+
+    def move_player(self, x1, y1):
+        x, y = self.player.move(x1, y1)
+        monster = self.field[x][y]
+
+        if not monster:
+            return shlex.join([str(x), str(y)])
+        return shlex.join([str(x), str(y), monster.name, monster.word])
+    
+    def addmon(self, name, x, y, hp, word):
+        replaced = "False"
+        if self.field[x][y]:
+            replaced = "True"
+
+        self.field[x][y] = Monster(name, word, hp)
+        return shlex.join([name, str(x), str(y), word, replaced])
+
+    def attack(self, name, damage):
+        monster = self.field[self.player.x][self.player.y]
+
+        if not monster or monster.name != name:
+            return "False"
+
+        damage_res = min(monster.hp, damage)
+        monster.hp -= damage_res
+
+        if monster.hp == 0:
+            self.field[self.player.x][self.player.y] = None
+
+        return shlex.join(["True", str(damage_res), str(monster.hp)])
+    
+async def echo_client(reader, writer):
+    addr = writer.get_extra_info("peername")
+    print(f"Connected: {addr}")
+
+    game = Game()
+
+    while not reader.at_eof():
+        data = await reader.readline()
+        if not data:
+            break
+
+        args = shlex.split(data.decode().rstrip("\n"))
+        if not args:
+            continue
+
+        cmd = args[0]
+
+        if cmd == "move":
+            x1, y1 = int(args[1]), int(args[2])
+            response = game.move_player(x1, y1)
+
+        elif cmd == "addmon":
+            name = args[1]
+            word = args[1 + args.index('hello')]
+            hitpoints = args[1 + args.index('hp')]
+            hitpoints = int(hitpoints)
+            c_id = args.index('coords')
+            x, y = args[c_id + 1], args[c_id + 2]
+            x, y = int(x), int(y)
+
+            response = game.addmon(name, x, y, hitpoints, word)
+
+        elif cmd == "attack":
+            name, damage = args[1], args[2]
+            response = game.attack(name, int(damage))
+
+        else:
+            response = "Invalid command"
+
+        writer.write((response + "\n").encode())
+        await writer.drain()
+
+    print(f"Disconnected: {addr}")
+    writer.close()
+    await writer.wait_closed()
+
+async def main():
+    server = await asyncio.start_server(echo_client, "0.0.0.0", 1337)
+
+    async with server:
+        await server.serve_forever()
+
+
+if __name__ == "__main__":
+    asyncio.run(main())
-- 
2.50.1 (Apple Git-155)

